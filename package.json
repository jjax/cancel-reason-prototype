{
  "name": "cancel-reason-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tsuklio cancel-subscription prototype with a TypeSafe (Jev) retention-offer judgment",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test \"test/**/*.test.mjs\""
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@typesafe-ai/sdk": "^0.6.0"
  }
}
